# 高德地图配置说明
#
# 只需一个 Key：你在控制台创建的「Web服务 / 地址解析」类型即可。
#
# 配置位置：项目根目录 .env
#   AMAP_WEB_SERVICE_KEY=435735c3b424dc4120fed1bbe85f7766
#
# 不需要再去找「Web端(JS API)」Key。
# 地图底图已改为 Leaflet + 高德瓦片，与后端地理编码坐标系一致。
#
# 若将来要在高德控制台添加 Web端 Key（可选，非必须）：
#   我的应用 → 你的应用 → 添加 Key → 服务平台选「Web端」
